// packages/api/routes/notification.routes.js
// User notification routes
const express = require('express');
const router = express.Router();
const { authenticate } = require('../../middleware');

// Notification routes are handled inline in packages/api/index.js
// This file exists as a placeholder for future notification endpoints

module.exports = router;
